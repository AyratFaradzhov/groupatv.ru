# 🔄 НОРМАЛИЗАЦИЯ ФАЙЛОВ И ПУТЕЙ

## 📋 ОПИСАНИЕ

Скрипт `normalize-files.js` выполняет **ПОЛНУЮ НОРМАЛИЗАЦИЮ** всех файлов изображений и путей в `products.json`.

## 🎯 ПРОБЛЕМЫ, КОТОРЫЕ РЕШАЕТ

### ❌ До нормализации:
- Пути содержат кириллицу: `assets/images/Damla Bears ( Mishki ) 80gr/...`
- Пути содержат пробелы и скобки
- Реальные имена файлов не совпадают с `image` в JSON
- Изображения не загружаются на фронтенде

### ✅ После нормализации:
- Все пути в латинице: `assets/images/products/damla/damla-bears-80gr.webp`
- Все имена в kebab-case
- Все пути существуют и корректны
- Изображения загружаются без проблем

## 🔧 ЧТО ДЕЛАЕТ СКРИПТ

### 1. Сканирование
- ✅ Рекурсивно сканирует `assets/images/` и `foods/`
- ✅ Находит все изображения: `.webp`, `.png`, `.jpg`, `.jpeg`
- ✅ Сопоставляет файлы с товарами в `products.json`

### 2. Нормализация имен
- ✅ Транслитерация кириллицы → латиница
- ✅ Удаление спецсимволов (пробелы, скобки, и т.д.)
- ✅ Преобразование в kebab-case
- ✅ Генерация имен на основе: brand + type + flavor + weight

### 3. Структура папок
Создает нормализованную структуру:
```
assets/images/products/
  ├── damla/
  │   ├── damla-bears-80gr.webp
  │   ├── damla-klubnika-15gr.webp
  │   └── ...
  ├── jimmy/
  │   ├── jimmy-belts-75gr.webp
  │   └── ...
  └── ...
```

### 4. Обновление JSON
- ✅ Обновляет все пути `image` в `products.json`
- ✅ Гарантирует, что все пути существуют
- ✅ Создает backup перед изменениями

### 5. Защита от коллизий
- ✅ Если имя уже используется → добавляет суффикс `-v1`, `-v2`
- ✅ Не перезаписывает существующие файлы
- ✅ Проверяет использование файлов другими товарами

## 🚀 ЗАПУСК

### Вариант 1: Через батник (Windows)
```bash
scripts\run-normalize.bat
```

### Вариант 2: Напрямую через Node.js
```bash
node scripts\normalize-files.js
```

## 📊 ОТЧЕТ

После выполнения скрипт выводит:

1. **Основная статистика:**
   - Файлов найдено
   - Файлов переименовано/скопировано
   - Путей обновлено в JSON
   - Файлов не найдено
   - Коллизий имен
   - Ошибок
   - Отсутствующих путей после нормализации

2. **Список проблем:**
   - Файлы не найдены (требуют ручной проверки)
   - Коллизии имен (решены автоматически)
   - Ошибки обработки

3. **Финальная проверка:**
   - Все ли пути существуют после нормализации

## 📁 ФАЙЛЫ

- `scripts/normalize-files.js` - основной скрипт
- `scripts/normalize-report.json` - детальный отчет
- `data/products.backup-normalize.json` - backup перед изменениями
- `data/products.json` - обновленный файл с нормализованными путями

## ⚙️ ПРАВИЛА НОРМАЛИЗАЦИИ

### Имена файлов:
- ✅ Только латиница
- ✅ kebab-case (маленькие буквы, дефисы)
- ✅ Без пробелов, скобок, спецсимволов
- ✅ Расширение `.webp`

### Примеры:
```
❌ "Damla Bears ( Mishki ) 80gr.webp"
✅ "damla-bears-80gr.webp"

❌ "Jimmy belts 75gr.webp"
✅ "jimmy-belts-75gr.webp"

❌ "Клубника 15г.webp"
✅ "klubnika-15gr.webp"
```

### Структура путей:
```
❌ "assets/images/Damla Bears ( Mishki ) 80gr/Damla Bears ( Mishki ) 80gr.webp"
✅ "assets/images/products/damla/damla-bears-80gr.webp"
```

## ⚠️ ВАЖНО

1. **Backup создается автоматически** перед любыми изменениями
2. **Исходные файлы в `foods/` НЕ удаляются** (это исходник)
3. **Файлы копируются**, а не перемещаются (безопасно)
4. **Все пути проверяются** после нормализации

## 🎯 РЕЗУЛЬТАТ

После выполнения:
- ✅ Все изображения имеют нормализованные имена
- ✅ Все пути в `products.json` в латинице
- ✅ Все пути существуют и корректны
- ✅ Изображения загружаются на фронтенде

---

**Дата создания**: 2025-01-XX
**Версия**: 1.0 (нормализация файлов и путей)




