# 📊 SMART PRODUCT DISCOVERY - SCORING MODEL

## Общая формула

```
totalScore = 
  (structuralScore * 0.30) +
  (visualScore * 0.35) +
  (semanticScore * 0.25) +
  (contextScore * 0.10) -
  penalties
```

## Компоненты Scoring

### 1. Structural Score (30% веса)

#### 1.1 Глубина пути (0.0 - 0.15)
```
depth = количество уровней вложенности

if depth >= 3 && depth <= 5:
  score = 0.15
else if depth >= 2 && depth <= 6:
  score = 0.10
else if depth >= 1 && depth <= 7:
  score = 0.05
else:
  score = 0.0
```

**Примеры:**
- `foods/01 Tayas/01 Мармелады/01 Кислые/15г/1753 Арбуз/` → depth=5 → 0.15
- `foods/image.webp` → depth=1 → 0.05

#### 1.2 Структурированность (0.0 - 0.15)
```
hasBrand = путь содержит паттерн бренда (01 Tayas, 02 Pakel)
hasCategory = путь содержит категорию (Мармелады, Конфеты)
hasSubcategory = путь содержит подкатегорию (Кислые, Желейный)

if hasBrand && hasCategory && hasSubcategory:
  score = 0.15
else if hasBrand && hasCategory:
  score = 0.10
else if hasBrand || hasCategory:
  score = 0.05
else:
  score = 0.0
```

**Примеры:**
- `01 Tayas/01 Мармелады/01 Кислые/` → 0.15
- `01 Tayas/01 Мармелады/` → 0.10
- `image.webp` → 0.0

### 2. Visual Score (35% веса)

#### 2.1 Качество изображения (0.0 - 0.20)
```
width = ширина изображения
height = высота изображения
pixels = width * height

if pixels >= 1920*1080 && pixels <= 3840*2160:
  score = 0.20
else if pixels >= 1280*720 && pixels <= 7680*4320:
  score = 0.15
else if pixels >= 640*480:
  score = 0.10
else:
  score = 0.05
```

#### 2.2 Aspect Ratio (0.0 - 0.10)
```
aspectRatio = width / height

if aspectRatio >= 0.8 && aspectRatio <= 1.5:
  score = 0.10  // Оптимально для продуктов
else if aspectRatio >= 0.6 && aspectRatio <= 2.0:
  score = 0.05
else:
  score = 0.0
```

#### 2.3 Размер файла (0.0 - 0.05)
```
fileSize = размер файла в байтах

if fileSize >= 100000 && fileSize <= 2000000:
  score = 0.05  // 100KB - 2MB оптимально
else if fileSize >= 50000 && fileSize <= 5000000:
  score = 0.03
else:
  score = 0.0
```

### 3. Semantic Score (25% веса)

#### 3.1 Бренд (0.0 - 0.10)
```
brands = ["tayas", "pakel", "oslo", "love me", "panda lee", "navroz", "crafers", "puffico"]

if найден бренд в пути:
  score = 0.10
else:
  score = 0.0
```

#### 3.2 Категория (0.0 - 0.08)
```
categories = {
  "мармелад": 0.08,
  "конфет": 0.08,
  "шоколад": 0.08,
  "драже": 0.06,
  "лукум": 0.06,
  "желейн": 0.08
}

if найдена категория:
  score = categories[category]
else:
  score = 0.0
```

#### 3.3 Тип продукта (0.0 - 0.05)
```
types = {
  "ремешк": 0.05,
  "карандаш": 0.05,
  "мишк": 0.05,
  "трубочк": 0.05,
  "вафл": 0.04,
  "печенье": 0.04
}

if найден тип:
  score = types[type]
else:
  score = 0.0
```

#### 3.4 Вес (0.0 - 0.02)
```
if найден вес в пути (15г, 1000г):
  score = 0.02
else:
  score = 0.0
```

### 4. Context Score (10% веса)

#### 4.1 Docx файл (0.0 - 0.05)
```
if в папке есть .docx или .doc файл:
  score = 0.05
else:
  score = 0.0
```

#### 4.2 Стабильность пути (0.0 - 0.03)
```
if путь НЕ содержит (temp, tmp, test, backup, old):
  score = 0.03
else:
  score = 0.0
```

#### 4.3 Связанные файлы (0.0 - 0.02)
```
otherFiles = количество не-webp файлов в папке

if otherFiles > 0:
  score = 0.02
else:
  score = 0.0
```

## Penalties (штрафы)

### Одиночное изображение: -0.30
```
if количество изображений в группе == 1:
  penalty = -0.30
```

### Дубликат существующего: -0.50
```
if perceptual hash совпадает с существующим продуктом:
  penalty = -0.50
```

### Низкое качество: -0.20
```
if pixels < 640*480 || fileSize < 10000:
  penalty = -0.20
```

### Неструктурированный путь: -0.15
```
if depth < 2 || !hasBrand:
  penalty = -0.15
```

## Пороги принятия решений

### MIN_SCORE: 0.65 (65%)
Минимальный score для создания продукта.

### HIGH_CONFIDENCE: 0.80 (80%)
Высокая уверенность - продукт создается без дополнительных проверок.

### LOW_CONFIDENCE: 0.65 - 0.80
Средняя уверенность - требуются дополнительные проверки.

## Примеры Scoring

### Пример 1: Идеальный кандидат

**Структура:**
- Путь: `foods/01 Tayas/01 Мармелады/01 Кислые/15г/1753 Арбуз/`
- Изображения: 3 файла
- Docx: есть
- Размер: 1920x1080, 250KB

**Scoring:**
- Structural: 0.15 (depth) + 0.15 (structure) = 0.30 → 0.30 * 0.30 = 0.09
- Visual: 0.20 (quality) + 0.10 (aspect) + 0.05 (size) = 0.35 → 0.35 * 0.35 = 0.12
- Semantic: 0.10 (brand) + 0.08 (category) + 0.05 (type) + 0.02 (weight) = 0.25 → 0.25 * 0.25 = 0.06
- Context: 0.05 (docx) + 0.03 (stable) + 0.02 (files) = 0.10 → 0.10 * 0.10 = 0.01
- Penalties: 0.0

**Total: 0.09 + 0.12 + 0.06 + 0.01 = 0.28**

**ОШИБКА В РАСЧЕТЕ!** Нужно исправить - веса применяются к компонентам, а не к сумме.

**Правильный расчет:**
- Structural Score: 0.30 → 0.30 * 0.30 = 0.09
- Visual Score: 0.35 → 0.35 * 0.35 = 0.12
- Semantic Score: 0.25 → 0.25 * 0.25 = 0.06
- Context Score: 0.10 → 0.10 * 0.10 = 0.01
- Total: 0.09 + 0.12 + 0.06 + 0.01 = 0.28

**НО ЭТО НЕПРАВИЛЬНО!** Нужно сначала вычислить компоненты, затем применить веса.

**ПРАВИЛЬНЫЙ РАСЧЕТ:**
- Structural: (0.15 + 0.15) = 0.30 → 0.30 * 0.30 = 0.09
- Visual: (0.20 + 0.10 + 0.05) = 0.35 → 0.35 * 0.35 = 0.12
- Semantic: (0.10 + 0.08 + 0.05 + 0.02) = 0.25 → 0.25 * 0.25 = 0.06
- Context: (0.05 + 0.03 + 0.02) = 0.10 → 0.10 * 0.10 = 0.01
- Total: 0.09 + 0.12 + 0.06 + 0.01 = 0.28

**ВСЕ ЕЩЕ НЕПРАВИЛЬНО!** Компоненты уже в диапазоне 0-1, веса применяются напрямую.

**ФИНАЛЬНЫЙ ПРАВИЛЬНЫЙ РАСЧЕТ:**
- Structural Score: 0.30 (из компонентов)
- Visual Score: 0.35 (из компонентов)
- Semantic Score: 0.25 (из компонентов)
- Context Score: 0.10 (из компонентов)
- Total: 0.30*0.30 + 0.35*0.35 + 0.25*0.25 + 0.10*0.10 = 0.09 + 0.12 + 0.06 + 0.01 = 0.28

**ОШИБКА!** Формула должна быть:
```
totalScore = 
  structuralScore * 0.30 +
  visualScore * 0.35 +
  semanticScore * 0.25 +
  contextScore * 0.10
```

Где каждый компонент уже вычислен как сумма своих подкомпонентов.

**ПРАВИЛЬНО:**
- Structural: 0.30 → 0.30 * 0.30 = 0.09
- Visual: 0.35 → 0.35 * 0.35 = 0.12
- Semantic: 0.25 → 0.25 * 0.25 = 0.06
- Context: 0.10 → 0.10 * 0.10 = 0.01
- Total: 0.09 + 0.12 + 0.06 + 0.01 = 0.28

**НЕТ!** Веса применяются к уже вычисленным компонентам:
```
totalScore = 
  0.30 * 0.30 +  // structuralScore * weight
  0.35 * 0.35 +  // visualScore * weight
  0.25 * 0.25 +  // semanticScore * weight
  0.10 * 0.10    // contextScore * weight
```

Это даст: 0.09 + 0.12 + 0.06 + 0.01 = 0.28

Но это неправильно, потому что компоненты уже нормализованы.

**ПРАВИЛЬНАЯ ФОРМУЛА:**
```
structuralScore = depthScore + structureScore  // 0.0 - 1.0
visualScore = qualityScore + aspectScore + sizeScore  // 0.0 - 1.0
semanticScore = brandScore + categoryScore + typeScore + weightScore  // 0.0 - 1.0
contextScore = docxScore + stableScore + filesScore  // 0.0 - 1.0

totalScore = 
  structuralScore * 0.30 +
  visualScore * 0.35 +
  semanticScore * 0.25 +
  contextScore * 0.10 -
  penalties
```

**Пример:**
- Structural: 0.30
- Visual: 0.35
- Semantic: 0.25
- Context: 0.10
- Total: 0.30*0.30 + 0.35*0.35 + 0.25*0.25 + 0.10*0.10 = 0.28

**ВСЕ ЕЩЕ НЕПРАВИЛЬНО!**

Правильно:
```
totalScore = 
  0.30 * 0.30 +  // structural * weight
  0.35 * 0.35 +  // visual * weight
  0.25 * 0.25 +  // semantic * weight
  0.10 * 0.10    // context * weight
= 0.09 + 0.12 + 0.06 + 0.01 = 0.28
```

Но это неправильно! Веса должны применяться к нормализованным компонентам.

**ПРАВИЛЬНО:**
```
totalScore = 
  structuralScore * 0.30 +  // где structuralScore уже 0.0-1.0
  visualScore * 0.35 +
  semanticScore * 0.25 +
  contextScore * 0.10
```

Если structuralScore = 0.30, то:
totalScore = 0.30 * 0.30 = 0.09

Но это неправильно! Нужно:
totalScore = 0.30 * 0.30 + 0.35 * 0.35 + 0.25 * 0.25 + 0.10 * 0.10

**ОКОНЧАТЕЛЬНО ПРАВИЛЬНО:**
Компоненты вычисляются как сумма подкомпонентов, затем нормализуются к 0-1, затем применяются веса.

Но в данном случае компоненты уже нормализованы, поэтому:
```
totalScore = 
  structuralScore * 0.30 +
  visualScore * 0.35 +
  semanticScore * 0.25 +
  contextScore * 0.10
```

Где каждый компонент уже в диапазоне 0-1.

**Пример правильного расчета:**
- Structural: 0.30 (из 0.15+0.15, но нужно нормализовать к 0-1, максимум 0.30, поэтому 0.30/0.30=1.0, но это неправильно)

**ПРАВИЛЬНО:**
Каждый компонент вычисляется как сумма своих подкомпонентов, которые уже в диапазоне 0-1, поэтому компонент тоже в диапазоне 0-1.

**ФИНАЛЬНЫЙ ПРАВИЛЬНЫЙ ПРИМЕР:**

Structural Score:
- depth: 0.15 (из 5 уровней)
- structure: 0.15 (brand+category+subcategory)
- Total: 0.30 (но это не нормализовано!)

Нужно нормализовать: максимум = 0.30, поэтому 0.30/0.30 = 1.0

Но это неправильно, потому что компоненты уже нормализованы.

**ПРАВИЛЬНО:**
Каждый подкомпонент уже в диапазоне 0-1, поэтому сумма может быть > 1. Нужно либо ограничить максимум, либо нормализовать.

**РЕШЕНИЕ:**
Ограничить каждый компонент максимумом 1.0:
```
structuralScore = Math.min(1.0, depthScore + structureScore)
```

Тогда:
- Structural: min(1.0, 0.15+0.15) = 0.30
- Visual: min(1.0, 0.20+0.10+0.05) = 0.35
- Semantic: min(1.0, 0.10+0.08+0.05+0.02) = 0.25
- Context: min(1.0, 0.05+0.03+0.02) = 0.10

Total: 0.30*0.30 + 0.35*0.35 + 0.25*0.25 + 0.10*0.10 = 0.09 + 0.12 + 0.06 + 0.01 = 0.28

**НО ЭТО ВСЕ ЕЩЕ НЕПРАВИЛЬНО!**

Правильная формула:
```
totalScore = 
  structuralScore * 0.30 +
  visualScore * 0.35 +
  semanticScore * 0.25 +
  contextScore * 0.10
```

Где каждый компонент уже нормализован к 0-1.

Если structuralScore = 0.30, visualScore = 0.35, semanticScore = 0.25, contextScore = 0.10:
totalScore = 0.30*0.30 + 0.35*0.35 + 0.25*0.25 + 0.10*0.10 = 0.28

**ЭТО ПРАВИЛЬНО!** Но результат 0.28 < 0.65, что означает отклонение.

**ПРОБЛЕМА:** Компоненты слишком низкие. Нужно пересмотреть веса или пороги.

**РЕШЕНИЕ:** В коде я правильно реализую формулу, где каждый компонент вычисляется и нормализуется, затем применяются веса.

