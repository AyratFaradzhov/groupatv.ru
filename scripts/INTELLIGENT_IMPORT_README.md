# 🧠 INTELLIGENT PRODUCTS IMPORT SYSTEM

## Описание

Комплексная двухэтапная система для санации и интеллектуального импорта продуктов из директории `foods/` в `products.json`.

## Архитектура

### ЭТАП 1: САНАЦИЯ (Sanitation)
Удаление некорректных продуктов, созданных предыдущими версиями скриптов.

### ЭТАП 2: INTELLIGENT INGESTION
Умный импорт с использованием scoring-модели на основе структурного, семантического, визуального и контекстного анализа.

## Критические принципы

✅ **НЕ изменяет**:
- `brands` массив
- `categories` объект
- Общую структуру `products.json`

✅ **Работает ТОЛЬКО** с массивом `products`

✅ **Идемпотентность**: повторный запуск не создает дубликаты

✅ **Immutable slug**: ID продукта формируется из устойчивых признаков и не меняется

✅ **Scoring-модель**: продукт создается ТОЛЬКО при прохождении порога

## Запуск

### Windows (bat-файл)
Дважды кликните на `scripts/run-intelligent-import.bat`

### Командная строка
```cmd
cd "C:\Users\Galax\OneDrive\Рабочий стол\Atv_gro"
node scripts\intelligent-products-import.js
```

## ЭТАП 1: САНАЦИЯ

### Критерии удаления продуктов

1. **ID с суффиксами (-1, -2, -3)**
   - Удаляются продукты с ID вида `product-name-1`, `product-name-2`
   - Если есть базовая версия без суффикса, удаляется версия с суффиксом

2. **Дубликаты по изображениям**
   - Если несколько продуктов ссылаются на одно изображение
   - Оставляется продукт с более коротким ID или существующим файлом
   - Остальные удаляются

3. **Несуществующие изображения**
   - Продукты, у которых файл изображения не существует
   - Автоматически удаляются

4. **Неподтвержденные продукты** (в разработке)
   - Продукты, которые не могут быть подтверждены структурой `foods/`

### Результат санации

- ✅ Удалены все мусорные продукты
- ✅ Остались только подтвержденные записи
- ✅ `brands` и `categories` не затронуты

## ЭТАП 2: INTELLIGENT INGESTION

### Scoring-модель

Каждый кандидат (изображение из `foods/`) оценивается по 4 критериям:

#### 1. Структурный анализ (30% веса)
- **Глубина расположения**: оптимально 3-5 уровней вложенности
- **Структурированность пути**: наличие бренда → категория → подкатегория
- **Множественные изображения**: наличие 2-5 изображений в папке (признак продукта)

#### 2. Семантический анализ (25% веса)
- **Извлечение бренда**: из структуры папок
- **Извлечение категории**: мармелад, конфеты, шоколад и т.д.
- **Извлечение типа**: ремешки, карандаши, мишки, трубочки
- **Извлечение веса**: 15г, 1000г и т.д.
- **Извлечение вкуса**: клубника, арбуз, кола и т.д.
- **Штраф за числовые коды**: использование кодов типа 8706 в slug

#### 3. Визуальный анализ (35% веса)
- **Размер файла**: оптимально 50KB - 2MB
- **Aspect ratio**: оптимально 0.8 - 1.5 для продуктов
- **Perceptual hash**: для обнаружения дубликатов
- **Валидность изображения**: проверка на корректный формат

#### 4. Контекстный анализ (10% веса)
- **Наличие docx файла**: признак продукта с описанием
- **Связанные изображения**: другие изображения в той же папке
- **Стабильность пути**: отсутствие временных/служебных папок

### Пороги

- **Минимальный score**: 0.65 (65%)
- **Порог схожести изображений**: 0.85 (85%)
- **Минимальный размер изображения**: 5KB
- **Максимальный размер изображения**: 10MB

### Создание immutable slug

Slug формируется ТОЛЬКО из устойчивых признаков:
1. Бренд (из структуры папок)
2. Тип продукта (ремешки, карандаши и т.д.)
3. Вкус (клубника, арбуз и т.д.)
4. Вес (15gr, 1000gr и т.д.)

**НЕ используется**:
- ❌ Имя файла изображения
- ❌ Числовые коды (8706, PL1030 и т.д.)
- ❌ Временные или служебные данные

### Обработка дубликатов

- **Визуальное сравнение**: perceptual hash + сравнение файлов
- **Проверка существующих**: сравнение с уже импортированными продуктами
- **Штраф за дубликаты**: -0.5 к общему score

## Конфигурация

Все параметры настраиваются в объекте `CONFIG`:

```javascript
CONFIG = {
  SCORING: {
    MIN_SCORE: 0.65,           // Минимальный score
    STRUCTURAL_WEIGHT: 0.30,    // Вес структурного анализа
    SEMANTIC_WEIGHT: 0.25,      // Вес семантического анализа
    VISUAL_WEIGHT: 0.35,        // Вес визуального анализа
    CONTEXT_WEIGHT: 0.10        // Вес контекстного анализа
  },
  SANITATION: {
    REMOVE_SUFFIX_IDS: true,
    REMOVE_DUPLICATE_IMAGES: true,
    REMOVE_MISSING_IMAGES: true,
    REMOVE_UNVERIFIED: true
  },
  VISUAL: {
    PHASH_SIZE: 16,
    SIMILARITY_THRESHOLD: 0.85,
    MIN_ASPECT_RATIO: 0.5,
    MAX_ASPECT_RATIO: 2.0
  }
}
```

## Результат работы

### Статистика

После выполнения скрипт выводит:
- Количество удаленных продуктов (санация)
- Количество оставшихся продуктов
- Количество найденных кандидатов
- Количество добавленных новых продуктов
- Общее количество продуктов в файле

### Backup

Автоматически создается backup перед любыми изменениями:
- `data/products.backup-{timestamp}.json`

## Важно

1. **Backup создается автоматически** перед запуском
2. **Идемпотентность**: можно запускать многократно без создания дубликатов
3. **Immutable slug**: ID продуктов не меняются при повторных запусках
4. **Только подтвержденные продукты**: создаются только продукты, прошедшие все проверки
5. **Автоматическое копирование изображений**: изображения копируются в `assets/images/products/`

## Примеры

### Пример 1: Удаление дубликата

**ДО:**
```json
{
  "id": "tayas-remeshki-klubnika-15gr",
  "image": "assets/images/products/remeshki-klubnika-15gr/image.webp"
},
{
  "id": "tayas-remeshki-klubnika-15gr-1",
  "image": "assets/images/products/remeshki-klubnika-15gr/image.webp"
}
```

**ПОСЛЕ:**
```json
{
  "id": "tayas-remeshki-klubnika-15gr",
  "image": "assets/images/products/remeshki-klubnika-15gr/image.webp"
}
```

### Пример 2: Создание нового продукта

**Кандидат из foods:**
- Путь: `foods/01 Tayas/01 Мармелады/01 Кислые Мармелады/15 г DAMLA Кислые Мармелады ( ремешки )/1753 Арбуз 1.webp`
- Score: 0.72 (прошел порог 0.65)

**Созданный продукт:**
```json
{
  "id": "tayas-remeshki-arbuz-15gr",
  "name": "ремешки арбуз 15gr",
  "category": "marmalade",
  "brand": "TAYAS",
  "image": "assets/images/products/tayas-remeshki-arbuz-15gr/tayas-remeshki-arbuz-15gr.webp"
}
```

## Требования

- Node.js (версия 12 или выше)
- Только встроенные модули Node.js (fs, path, crypto)
- Директория `foods/` с изображениями продуктов
- Файл `data/products.json`

## Развитие

### Планируемые улучшения

1. **Полноценный perceptual hash**: использование библиотеки для более точного сравнения
2. **Парсинг WebP**: извлечение реальных размеров изображений
3. **Проверка соответствия foods**: детальная валидация структуры
4. **Машинное обучение**: обучение модели на примерах для улучшения scoring
5. **Пакетная обработка**: оптимизация для больших объемов данных


