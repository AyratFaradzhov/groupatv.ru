# 🔥 АГРЕССИВНОЕ СКАНИРОВАНИЕ ПРОДУКТОВ

## 📋 ОПИСАНИЕ

Скрипт `aggressive-scan.js` выполняет **ПОЛНЫЙ РЕСКАН** всех файлов изображений в папке `foods/` и синхронизирует их с `products.json`.

## 🎯 ОСОБЕННОСТИ

### 1. Агрессивное сканирование
- ✅ Сканирует **ВСЕ** файлы: `.webp`, `.png`, `.jpg`, `.jpeg`
- ✅ Рекурсивно проходит по **ВСЕЙ** иерархии папок
- ✅ **КАЖДЫЙ файл = отдельный товар** (без автоматического объединения)
- ✅ Не исключает файлы по имени (включая `(1)`, `(2)`, и т.д.)

### 2. Извлечение данных
- **Brand** - из верхних уровней папок и имени файла
- **Category** - из структуры папок
- **Type** - belts, pencils, bears, tubes, wafers, cubes и т.д.
- **Weight** - из имени файла/папки (15gr, 80gr, 1000gr, 1kg)
- **Flavors** - из имени файла/папки (RU + EN)

### 3. Уникальность товаров
Товар считается уникальным, если отличается **ХОТЯ БЫ ОДНИМ**:
- вес
- вкус
- формат (type)
- изображение
- папка-источник

### 4. Расширенные поля
Для каждого продукта добавляются:
- `sourcePath` - полный путь к исходному файлу в `foods/`
- `detectedMeta` - объект с извлеченными данными
- `confidenceScore` - оценка уверенности (0-1)
- `importedBy: "cursor-framework-mode"`

### 5. Автоматическое обновление
- ✅ Создает backup `products.backup-aggressive.json`
- ✅ Добавляет новые товары
- ✅ Обновляет существующие товары
- ✅ Добавляет новые бренды в `brands[]`
- ✅ Добавляет новые категории в `categories{}`

## 🚀 ЗАПУСК

### Вариант 1: Через батник (Windows)
```bash
scripts\run-aggressive-scan.bat
```

### Вариант 2: Напрямую через Node.js
```bash
node scripts\aggressive-scan.js
```

### Вариант 3: Через npm (если настроен)
```bash
npm run scan
```

## 📊 ОТЧЕТ

После выполнения скрипт выводит:

1. **Основная статистика:**
   - Файлов найдено
   - Товаров ДО
   - Товаров ПОСЛЕ
   - Добавлено новых
   - Обновлено существующих
   - Новых брендов
   - Новых категорий
   - Товаров с низким confidence (<0.6)

2. **Таблица по брендам, категориям, типам и весу:**
   ```
   Бренд | Категория | Тип | Вес | Количество
   ```

3. **Список товаров с низким confidence:**
   - ID товара
   - Имя файла
   - Confidence score
   - Извлеченные данные

## 📁 ФАЙЛЫ

- `scripts/aggressive-scan.js` - основной скрипт
- `scripts/aggressive-scan-report.json` - детальный отчет о найденных файлах
- `data/products.backup-aggressive.json` - backup перед изменениями
- `data/products.json` - обновленный файл с товарами

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Зависимости
- `fast-glob` - для быстрого поиска файлов (опционально, есть fallback)
- Стандартные модули Node.js: `fs`, `path`

### Fallback
Если `fast-glob` не установлен, используется рекурсивное сканирование через `fs.readdirSync()`.

### Маппинги
Скрипт использует предопределенные маппинги для:
- Брендов (BRAND_MAP)
- Категорий (CATEGORY_MAP)
- Типов (TYPE_MAP)
- Вкусов (FLAVOR_MAP)

## ⚠️ ВАЖНО

1. **Backup создается автоматически** перед любыми изменениями
2. **Каждый файл = отдельный товар** - не объединяются автоматически
3. **Confidence score** помогает выявить товары, требующие ручной проверки
4. **Расширенные поля** сохраняются для последующего анализа

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После выполнения должно быть найдено **~200+ товаров** (вместо ~60 из предыдущего скана).

---

**Дата создания**: 2025-01-XX
**Версия**: 1.0 (агрессивное сканирование)




