# 🔄 Скрипт синхронизации продукции из foods в products.json

## Описание

Скрипт `sync-foods-to-products.js` автоматически сканирует директорию `foods/`, находит всю продукцию и добавляет её в файл `data/products.json` с правильной структурой, категориями и тегами.

## Возможности

✅ **Автоматическое сканирование** всей директории `foods/`  
✅ **Маппинг категорий** из структуры папок в категории products.json  
✅ **Маппинг брендов** в правильные названия брендов  
✅ **Очистка названий** от кодов продуктов  
✅ **Автоматическое создание ID** для продуктов  
✅ **Генерация тегов** на основе категории, вкуса и типа продукта  
✅ **Поиск изображений** в существующих папках  
✅ **Предотвращение дубликатов** - не добавляет продукты, которые уже есть  
✅ **Обновление брендов** - автоматически добавляет новые бренды  
✅ **Перевод названий** на английский язык  

## Запуск

### Способ 1: Через bat-файл (Windows)
Просто дважды кликните на файл `scripts/run-sync-foods.bat`

### Способ 2: Через командную строку
```cmd
cd "C:\Users\Galax\OneDrive\Рабочий стол\Atv_gro"
node scripts\sync-foods-to-products.js
```

### Способ 3: Через PowerShell
```powershell
Set-Location -LiteralPath 'C:\Users\Galax\OneDrive\Рабочий стол\Atv_gro'
node .\scripts\sync-foods-to-products.js
```

## Что делает скрипт

1. **Сканирует** директорию `foods/` используя логику из `display-products-by-categories.js`
2. **Обрабатывает каждый продукт**:
   - Очищает название от кодов (PL1030, AF0001, 1753 и т.д.)
   - Определяет категорию на основе структуры папок
   - Определяет бренд
   - Извлекает вес из названия
   - Создает уникальный ID
   - Генерирует теги
   - Ищет изображение в существующих папках
3. **Проверяет дубликаты**:
   - По ID продукта
   - По пути к изображению
4. **Добавляет новые продукты** в `products.json`
5. **Обновляет бренды** - добавляет новые бренды, если их нет
6. **Сохраняет** обновленный файл

## Маппинг категорий

| Категория в foods | Категория в products.json |
|-------------------|---------------------------|
| Мармелады | marmalade |
| Жевательные конфеты | candy |
| Конфеты | candy |
| Шоколады | chocolate |
| Драже | candy |
| Лукум | candy |
| Желейные десерты | jelly |
| Бисквитные пирожные | cookies |
| Хрустящие трубочки | cookies |

## Маппинг брендов

| Бренд в foods | Бренд в products.json |
|---------------|----------------------|
| TAYAS | TAYAS |
| PAKEL | PAKEL |
| ALIKHAN-ATA | SULIFA |
| PUFFICO | PUFFI |
| OSLO | OSLO |
| LOVE-ME | LOVE ME |
| PANDA-LEE | PANDA LEE |
| NAVROZ | NAVROZ |
| CRAFERS | CRAFERS |

## Формат ID продукта

ID создается в формате: `{brand-slug}-{name-slug}-{weight-slug}`

Примеры:
- `tayas-remeshki-klubnika-15gr`
- `pakel-shokolad-molochnyi-60gr`
- `love-me-kola-belts-1kg`

## Генерация тегов

Скрипт автоматически генерирует теги на основе:
- **Категории**: мармелад, конфеты, шоколад и т.д.
- **Вкуса**: клубника, арбуз, кола и т.д.
- **Типа**: ремешки, карандаши, трубочки и т.д.
- **Характеристик**: кислые, жевательные, хрустящие и т.д.

Теги создаются на русском и английском языках.

## Поиск изображений

Скрипт ищет изображения в следующих местах:
1. `assets/images/products/{product-id}/{image-name}.webp`
2. `assets/images/products/{product-id}/{product-id}.webp`
3. `assets/images/{product-name}/{image-name}.webp` (старая структура)

Если изображение не найдено, скрипт создает путь, но выводит предупреждение. В будущем можно добавить автоматическое копирование изображений.

## Предотвращение дубликатов

Скрипт не добавляет продукт, если:
- Уже существует продукт с таким же ID
- Уже используется изображение другим продуктом

## Структура создаваемого продукта

```json
{
  "id": "tayas-remeshki-klubnika-15gr",
  "name": "Кислые ремешки DAMLA, Клубника",
  "nameRu": "Кислые ремешки DAMLA, Клубника",
  "nameEn": "Sour Belts DAMLA, Strawberry",
  "category": "marmalade",
  "brand": "TAYAS",
  "image": "assets/images/products/tayas-remeshki-klubnika-15gr/image.webp",
  "descriptionKey": "tayas_remeshki_klubnika_15gr",
  "descriptionTextKey": "tayas_remeshki_klubnika_15gr_filling_text",
  "titleKey": "card__popular-item",
  "tags": [
    "мармелад",
    "клубника",
    "кислые",
    "ремешки",
    "marmalade",
    "strawberry",
    "sour",
    "belts"
  ],
  "weight": "15gr"
}
```

## Важно

- **Исходные файлы не изменяются** - скрипт только читает из `foods/` и записывает в `products.json`
- **Существующие продукты не удаляются** - скрипт только добавляет новые
- **Дубликаты определяются** по ID и пути к изображению
- **Бэкап не создается автоматически** - рекомендуется создать бэкап перед запуском
- **Изображения не копируются** - скрипт только ищет существующие или создает пути

## Рекомендации

1. **Создайте бэкап** перед запуском:
   ```cmd
   copy data\products.json data\products.backup.json
   ```

2. **Проверьте результат** после синхронизации:
   - Откройте `data/products.json`
   - Проверьте, что все продукты добавлены корректно
   - Проверьте, что категории и бренды правильные

3. **Скопируйте изображения** если нужно:
   - Используйте скрипт `scan-foods.js` для копирования изображений
   - Или скопируйте вручную в нужные папки

## Требования

- Node.js (версия 12 или выше)
- Только встроенные модули Node.js (fs, path) - дополнительные пакеты не требуются
- Файл `scripts/display-products-by-categories.js` должен существовать


