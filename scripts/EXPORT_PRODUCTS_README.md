# 📦 EXPORT PRODUCTS

## Описание

Скрипт собирает все товары из `out/products/*.json` в один файл `out/products.json`.

## Запуск

### Важно: сначала нужно сгенерировать товары
Перед экспортом убедитесь, что вы выполнили `npm run build` в `foods-tools-cursor`:
```bash
cd foods-tools-cursor/foods_tools_cursor
npm install
npm run build
cd ../..
```

### Через npm
```bash
npm run export
```

### С указанием пути вручную
Если директория находится в нестандартном месте:
```bash
npm run export -- --path "foods-tools-cursor/foods_tools_cursor/out/products"
```

### Напрямую
```bash
node scripts/exportProducts.js
```

### С указанием пути
```bash
node scripts/exportProducts.js --path "путь/к/out/products"
```

## Что делает скрипт

1. **Находит директорию** `out/products` (проверяет несколько возможных путей)
2. **Сканирует** все `.json` файлы в директории
3. **Исключает** служебный файл `_issues.json`
4. **Читает и парсит** каждый файл товара
5. **Собирает** все товары в массив
6. **Сохраняет** результат в `out/products.json`

## Требования

- Директория `out/products` должна существовать
- В ней должны быть файлы товаров (создаются после `npm run build` в foods-tools-cursor)

## Формат выходного файла

```json
[
  {
    "sku": "...",
    "name": "...",
    ...
  },
  {
    "sku": "...",
    "name": "...",
    ...
  }
]
```

## Обработка ошибок

- Если директория не найдена → выводит ошибку и список проверенных путей
- Если файл не может быть прочитан → пропускает его и выводит предупреждение
- Если файл содержит невалидный JSON → пропускает его и выводит предупреждение

## Статистика

После выполнения скрипт выводит:
- Количество найденных файлов
- Количество успешно прочитанных товаров
- Количество ошибок
- Размер выходного файла

## Важно

- Скрипт НЕ изменяет исходные файлы товаров
- Скрипт НЕ удаляет `_issues.json`
- Порядок товаров в массиве не гарантирован
- Используются только стандартные модули Node.js (fs, path)

