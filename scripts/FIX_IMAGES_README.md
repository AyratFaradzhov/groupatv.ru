# 📸 Исправление проблемы с изображениями

## Проблема
После синхронизации продукции из `foods/` в `products.json`, изображения не находились, потому что они не были скопированы из директории `foods/` в `assets/images/products/`.

## Решение
Скрипт `sync-foods-to-products.js` теперь **автоматически копирует изображения** из `foods/` в `assets/images/products/` при синхронизации.

## Что было исправлено

1. ✅ Добавлена функция `copyImageToProducts()` - автоматически копирует изображения
2. ✅ Улучшена функция `getImagePath()` - теперь копирует изображения, если они не найдены
3. ✅ Добавлена статистика по скопированным изображениям

## Как использовать

### Вариант 1: Запустить синхронизацию заново
Если вы уже запускали синхронизацию, но изображения не скопировались:

1. **Создайте бэкап** (на всякий случай):
   ```cmd
   copy data\products.json data\products.backup-before-fix.json
   ```

2. **Запустите синхронизацию**:
   ```cmd
   node scripts\sync-foods-to-products.js
   ```
   
   Или дважды кликните на `scripts\run-sync-foods.bat`

3. Скрипт автоматически:
   - Найдет все продукты в `foods/`
   - Скопирует изображения в `assets/images/products/{product-id}/`
   - Обновит пути в `products.json`

### Вариант 2: Использовать существующий скрипт scan-foods.js
Если хотите сначала скопировать все изображения, а потом синхронизировать:

1. **Скопируйте изображения**:
   ```cmd
   node scripts\scan-foods.js
   ```
   
   Или дважды кликните на `scripts\run-scan.bat`

2. **Затем синхронизируйте**:
   ```cmd
   node scripts\sync-foods-to-products.js
   ```

## Структура копирования

Изображения копируются в следующую структуру:
```
assets/images/products/
  └── {product-id}/
      └── {product-id}.webp
```

Например:
- `assets/images/products/tayas-remeshki-klubnika-15gr/tayas-remeshki-klubnika-15gr.webp`
- `assets/images/products/pakel-shokolad-molochnyi-60gr/pakel-shokolad-molochnyi-60gr.webp`

## Важно

- ✅ **Исходные файлы в `foods/` не удаляются** - скрипт только копирует
- ✅ **Существующие изображения не перезаписываются** - если файл уже есть, используется существующий
- ✅ **Автоматическое создание папок** - скрипт создает нужные директории автоматически
- ✅ **Обработка ошибок** - если копирование не удалось, скрипт продолжит работу и выведет предупреждение

## Проверка результата

После запуска скрипта проверьте:

1. **Изображения скопированы**:
   - Откройте `assets/images/products/`
   - Должны быть папки с названиями продуктов
   - В каждой папке должен быть файл `.webp`

2. **Пути в products.json правильные**:
   - Откройте `data/products.json`
   - Проверьте поле `image` у продуктов
   - Путь должен быть вида: `assets/images/products/{product-id}/{product-id}.webp`

3. **В браузере изображения отображаются**:
   - Откройте страницу с продуктами
   - Изображения должны загружаться

## Статистика

После запуска скрипт выведет статистику:
```
📊 Статистика:
   • Всего найдено продуктов: 150
   • Добавлено новых продуктов: 120
   • Пропущено продуктов: 30
   • Скопировано изображений: 120
   • Всего продуктов в файле: 270
   • Всего брендов: 9
```

## Если изображения все еще не находятся

1. **Проверьте пути** в `products.json` - они должны быть относительными от корня проекта
2. **Проверьте права доступа** - убедитесь, что скрипт может создавать папки и копировать файлы
3. **Проверьте формат файлов** - все изображения должны быть в формате `.webp`
4. **Запустите скрипт снова** - он пропустит уже существующие изображения


