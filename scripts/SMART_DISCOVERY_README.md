# 🧠 SMART PRODUCT DISCOVERY ENGINE

## Описание

Интеллектуальная система поиска и импорта продуктов из неструктурированной файловой системы. Это НЕ простой парсер, а система машинного обнаружения продуктов на основе множественных сигналов и визуального анализа.

## Архитектура

### 4-этапный Pipeline

```
DISCOVERY → CANDIDATE GROUPING → VISUAL ANALYSIS → DECISION ENGINE
```

#### 1️⃣ DISCOVERY
- Сканирует `foods/` рекурсивно
- Находит ВСЕ изображения (.webp)
- Извлекает метаданные (размеры, формат, размер файла)
- НИЧЕГО не импортирует на этом этапе

#### 2️⃣ CANDIDATE GROUPING
- Группирует изображения в `ProductCandidate`
- Использует структурные и визуальные сигналы
- Отбрасывает одиночные изображения (penalty)
- Выбирает главное изображение из группы

#### 3️⃣ VISUAL ANALYSIS
- Использует perceptual hash (blockhash-core или fallback)
- Определяет дубликаты с существующими продуктами
- Анализирует качество изображений
- Отличает маркетинговые изображения от товарных

#### 4️⃣ DECISION ENGINE
- Применяет scoring-модель
- Создает продукт ТОЛЬКО если score > 0.65
- Если есть сомнение → НЕ импортирует

## Установка зависимостей

```bash
npm install
```

Или вручную:
```bash
npm install fast-glob sharp image-size blockhash-core
```

**Примечание:** Если библиотеки не установлены, скрипт использует fallback-методы на встроенных модулях Node.js.

## Запуск

### Windows (bat-файл)
Дважды кликните на `scripts/run-smart-discovery.bat`

### Командная строка
```cmd
cd "C:\Users\Galax\OneDrive\Рабочий стол\Atv_gro"
node scripts\smart-product-discovery.js
```

## Scoring-модель

### Формула
```
totalScore = 
  (structuralScore * 0.30) +
  (visualScore * 0.35) +
  (semanticScore * 0.25) +
  (contextScore * 0.10) -
  penalties
```

### Компоненты

#### Structural Score (30%)
- **Глубина пути** (0.0 - 0.15): оптимально 3-5 уровней
- **Структурированность** (0.0 - 0.15): бренд → категория → подкатегория

#### Visual Score (35%)
- **Качество изображения** (0.0 - 0.20): разрешение, оптимально 1920x1080 - 3840x2160
- **Aspect ratio** (0.0 - 0.10): оптимально 0.8 - 1.5
- **Размер файла** (0.0 - 0.05): оптимально 100KB - 2MB

#### Semantic Score (25%)
- **Бренд** (0.0 - 0.10): найден в пути
- **Категория** (0.0 - 0.08): мармелад, конфеты, шоколад
- **Тип** (0.0 - 0.05): ремешки, карандаши, мишки
- **Вес** (0.0 - 0.02): найден в пути

#### Context Score (10%)
- **Docx файл** (0.0 - 0.05): наличие описания
- **Стабильность пути** (0.0 - 0.03): нет temp/tmp/test
- **Связанные файлы** (0.0 - 0.02): другие файлы в папке

### Penalties (штрафы)

- **Одиночное изображение**: -0.30
- **Дубликат существующего**: -0.50
- **Низкое качество**: -0.20
- **Неструктурированный путь**: -0.15

### Пороги

- **MIN_SCORE**: 0.65 (65%) - минимальный для создания
- **HIGH_CONFIDENCE**: 0.80 (80%) - высокая уверенность

## Особенности

### Группировка изображений

Изображения в одной папке группируются по визуальной схожести:
- Если perceptual hash похож (≥ 0.85) → варианты одного продукта
- Если perceptual hash отличается (< 0.50) → разные продукты

### Выбор главного изображения

Из группы выбирается одно главное по критериям:
1. Размер (40%): оптимально 100KB - 1MB
2. Aspect ratio (30%): оптимально 0.9 - 1.1
3. Имя файла (30%): без служебных слов

### Immutable Slug

Формируется ТОЛЬКО из устойчивых признаков:
1. Бренд (из структуры)
2. Тип продукта
3. Вкус/характеристика
4. Вес

**НЕ используется:**
- ❌ Имя файла
- ❌ Числовые коды (1753, 8542)
- ❌ Временные данные

### Идемпотентность

Повторный запуск безопасен:
- Проверка по immutable slug
- Проверка по perceptual hash
- Никаких -1, -2, -3 не создается

## Результат

✅ Импортируются ТОЛЬКО реальные продукты (score ≥ 0.65)
✅ Варианты упаковок/ракурсов группируются как один продукт
✅ Служебные изображения игнорируются
✅ Маркетинговые фото не создают отдельные продукты
✅ Нет дубликатов (проверка по perceptual hash)
✅ Slug immutable и предсказуемый
✅ Система масштабируема на сотни товаров

## Производительность

- **100 продуктов**: ~5-10 секунд
- **500 продуктов**: ~30-60 секунд
- **1000+ продуктов**: ~2-5 минут

## Конфигурация

Все параметры настраиваются в объекте `CONFIG`:

```javascript
CONFIG = {
  DISCOVERY: {
    PATTERNS: ['**/*.webp'],
    IGNORE_PATTERNS: ['**/* 2.webp', '**/* 3.webp'],
    MAX_FILE_SIZE: 50 * 1024 * 1024,
    MIN_FILE_SIZE: 1000
  },
  GROUPING: {
    MIN_IMAGES_IN_GROUP: 1,
    MAX_IMAGES_IN_GROUP: 15,
    SIMILARITY_THRESHOLD: 0.85
  },
  SCORING: {
    MIN_SCORE: 0.65,
    HIGH_CONFIDENCE: 0.80,
    WEIGHTS: {
      STRUCTURAL: 0.30,
      VISUAL: 0.35,
      SEMANTIC: 0.25,
      CONTEXT: 0.10
    }
  },
  VISUAL: {
    OPTIMAL_PIXELS: { min: 1920 * 1080, max: 3840 * 2160 },
    OPTIMAL_ASPECT_RATIO: { min: 0.8, max: 1.5 },
    OPTIMAL_FILE_SIZE: { min: 100000, max: 2000000 }
  }
}
```

## Отчеты

После выполнения скрипт выводит детальную статистику:
- Количество найденных изображений
- Количество созданных кандидатов
- Количество одобренных/отклоненных
- Причины отклонения
- Итоговое количество продуктов

## Требования

- Node.js (версия 12 или выше)
- Опционально: sharp, image-size, blockhash-core (для улучшенного анализа)
- Директория `foods/` с изображениями
- Файл `data/products.json`

## Развитие

### Планируемые улучшения

1. **Машинное обучение**: обучение модели на примерах
2. **Параллельная обработка**: использование worker threads
3. **Инкрементальное обновление**: обработка только новых файлов
4. **Расширенные метрики**: более точный perceptual hash
5. **Веб-интерфейс**: визуализация процесса и результатов

