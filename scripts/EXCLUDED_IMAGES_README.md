# 🚫 ИСКЛЮЧЕННЫЕ ИЗОБРАЖЕНИЯ

## 📋 ОПИСАНИЕ

Файл `excluded-images.json` содержит список изображений, которые **НЕ должны импортироваться** в `products.json`.

## ⚠️ ВАЖНО

- ✅ Эти изображения **удаляются** из существующих товаров
- ✅ Эти изображения **игнорируются** при будущих импортах
- ✅ Товары с этими изображениями **не создаются** автоматически

## 📁 ФАЙЛЫ

- `scripts/excluded-images.json` - список исключенных изображений
- `scripts/remove-products-by-images.js` - скрипт удаления товаров
- `scripts/EXCLUDED_IMAGES_README.md` - эта документация

## 🔧 ИСПОЛЬЗОВАНИЕ

### Удаление существующих товаров

```bash
node scripts\remove-products-by-images.js
```

Скрипт автоматически загрузит список из `excluded-images.json` и удалит все товары с указанными изображениями.

### Добавление новых исключений

1. Откройте `scripts/excluded-images.json`
2. Добавьте новый путь в массив `images`
3. Обновите поле `total` (количество изображений)
4. Обновите `lastUpdated` (дата обновления)

### Использование в скриптах импорта

При создании новых скриптов импорта проверяйте список исключений:

```javascript
const excludedImages = require('./excluded-images.json');

function shouldImport(imagePath) {
  return !excludedImages.images.includes(imagePath);
}
```

## 📊 ТЕКУЩИЙ СПИСОК

Всего исключено: **22 изображения**

Все изображения относятся к бренду **TAYAS**:
- Ремешки (belts) различных вкусов
- Мармелад (marmalade) различных вариантов

## 🔄 ОБНОВЛЕНИЕ

Последнее обновление: 2025-01-XX

---

**Примечание:** Этот файл используется для предотвращения импорта нежелательных товаров и автоматического удаления существующих.




